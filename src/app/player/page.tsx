import { PlayerExplorer } from "@/components/PlayerExplorer";

export const runtime = "nodejs";
export const dynamic = "force-dynamic";

export default function PlayerPage() {
  return <PlayerExplorer />;
}
